#include "main.h"

/**
 * _abs - Computes absolute value
 * @n: takes an interger
 *
 * Description: The program uses
 * Return: n if negative and -n if possitive
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
