#include "main.h"
/**
 * _isdigit - implementing empty function for the sake of dynamic library ex.
 * @c: String to be added
 *
 * Return: Number of bytes written.
 */
int _isdigit(int c)
{
        return (0);
}

/**
 * _strlen - implementing empty function for the sake of dynamic library ex.
 * @s: String to be added
 *
 * Return: Number of bytes written.
 */
int _strlen(char *s)
{
        return (0);
}
/**
 * _puts - implementing empty function for the sake of dynamic library ex.
 * @s: String to be added
 *
 * Return: Number of bytes written.
 */
void _puts(char *s)
{
        return;
}

/**
 * _strcpy - implementing empty function for the sake of dynamic library ex.
 * @dest: String to be added
 * @src:
 *
 * Return: New string
 */
char *_strcpy(char *dest, char *src)
{
        return (NULL);
}

/**
 * _atoi - implementing empty function for the sake of dynamic library ex.
 * @s: String to be added
 *
 * Return: New string
 */
int _atoi(char *s)
{
        return (0);
}

/**
 * _strcat - implementing empty function for the sake of dynamic library ex.
 * @dest: String to be added
 * @src:
 *
 * Return: New string
 */
char *_strcat(char *dest, char *src)
{
        return (NULL);
}

/**
 * _strncat - implementing empty function for the sake of dynamic library ex.
 * @dest: String to be added
 * @src:
 * @n:
 *
 * Return: New string
 */
char *_strncat(char *dest, char *src, int n)
{
        return (NULL);
}

/**
 * _strncpy - implementing empty function for the sake of dynamic library ex.
 * @dest: String to be added
 * @src:
 * @n:
 *
 * Return: New string
 */
char *_strncpy(char *dest, char *src, int n)
{
        return (NULL);
}


/**
 * _strcmp - implementing empty function for the sake of dynamic library ex.
 * @s1: String to be added
 * @s2:
 *
 * Return:
 */
int _strcmp(char *s1, char *s2)
{
        return (0);
}

/**
 * _memset - implementing empty function for the sake of dynamic library ex.
 * @s: String to be added
 * @b:
 * @n:
 *
 * Return: New string
 */
char *_memset(char *s, char b, unsigned int n)
{
        return (NULL);
}
/**
 * _memcpy - implementing empty function for the sake of dynamic library ex.
 * @dest: String to be added
 * @src:
 * @n:
 *
 * Return: New string
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
        return (NULL);
}

/**
 * _strchr - implementing empty function for the sake of dynamic library ex.
 * @c: String to be added
 * @s:
 *
 * Return: New string
 */
char *_strchr(char *s, char c)
{
        return (NULL);
}

/**
 * _strspn - implementing empty function for the sake of dynamic library ex.
 * @c: String to be added
 * @s:
 *
 * Return: New string
 */
unsigned int _strspn(char *s, char *accept)
{
        return (0);
}
/**
 * _strpbrk - implementing empty function for the sake of dynamic library ex.
 * @c: String to be added
 * @s:
 *
 * Return: New string
 */
char *_strpbrk(char *s, char *accept)
{
        return (NULL);
}

/**
 * _strstr - implementing empty function for the sake of dynamic library ex.
 * @haystack: String to be added
 * @needle:
 *
 * Return: New string
 */
char *_strstr(char *haystack, char *needle)
{
        return (NULL);
}
