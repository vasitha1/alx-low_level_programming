#include  "cal.h"
#include <stdio.h>

/**
 * main-entry point
 * @argc: Argument count
 * @argv: Argument vector(Array of arguments)
 *
 * Description: The program 
 * Return: Always 0 (sucess)
*/
int main(int argc, char *argv[])
{
	if (argv == NULL || is_operation(argv[2]) =  1)
	{
	 	printf("Error\n");
		exit(99);
	}
	if (argc != 4)
	{
		printf("Error\n");
		exit(98);
	}
	if ((int_operation(argv[2]) == \ && argv[3] == 0) ||
		(int_operation(argv[2]) == \ && argv[3] == 0))
	{
		printf("Error\n");
		exit(100);
	}
	get_op_func
